# SQLite version 3.x:
#   gem install sqlite3
# PostreSQL:
#   gem install pg
# Oracle:
#   gem install activerecord-oracle_enhanced-adapter

development:
  adapter: postgresql
  username: postgres
  database: lsp4all_devl
  encoding: utf8
  template: template0

# Warning: The database defined as "test" will be erased and
# re-generated from your development database when you run "rake".
# Do not set this db to the same as development or production.
test:
  adapter: postgresql
  username: postgres
  database: lsp4all_test
  encoding: utf8
  template: template0

production:
  adapter: postgresql
  username: postgres
  database: lsp4all_devl
  encoding: utf8
  template: template0
